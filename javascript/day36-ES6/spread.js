console.log(Math.max(10, 30));
let numbers = [15, 25, 20, 35, 12];
console.log(Math.max(numbers));//returns Nan

//spread
console.log(...numbers);//spread method can be used or passed only as an arguments to the paarticular
console.log(Math.max(...numbers));