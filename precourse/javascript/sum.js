/*
create a function that takes two arguments n1 and n2 whose values are 10 and 20 respectively, the function should return the sum of these two numbers
*/
function addSum(n1, n2) {
    var sum = n1 + n2;
    return('the sum  of n1 and n2 is' + sum);
    //return sum;
}
console.log(addSum(10,20));